
Create video with image and audio as input
currently done with converting the image to store the important checkpoints as numpy array and we can do use this as input for further tasks but the issue is this is not that accurate 

preprocessing dw pose is the main file for extraction features from the video and store it in the form of numpy array
